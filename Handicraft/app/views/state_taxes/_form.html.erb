<%= form_for(@state_tax) do |f| %>
  <% if @state_tax.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state_tax.errors.count, "error") %> prohibited this state_tax from being saved:</h2>

      <ul>
      <% @state_tax.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :state_code %><br>
    <%= f.text_field :state_code %>
  </div>
  <div class="field">
    <%= f.label :tax_rate %><br>
    <%= f.text_field :tax_rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
